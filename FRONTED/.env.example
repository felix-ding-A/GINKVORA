# Server-only secrets. Set these in Vercel; never commit real values.
SANITY_REVALIDATE_SECRET=
ISR_BYPASS_TOKEN=

# Use the Preview deployment URL only when testing a Preview webhook.
REVALIDATE_BASE_URL=https://ginkvora.com
